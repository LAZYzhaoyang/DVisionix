# 依赖的唯一来源是 `pyproject.toml` 的 `[project]` / `[project.optional-dependencies]`。
#
# 本文件只为兼容 `pip install -r requirements.txt` 的习惯而保留，**不在此声明任何版本** ——
# v1.0.0 曾出现本文件与 setup.py 双源漂移（timm>=0.9.0 vs >=0.16.0；pillow 只在单侧声明）。
#
# 需要哪些可选能力就选对应 extras：
#   -e .              仅核心依赖（torch / torchvision / numpy / pillow / opencv / pyyaml / tensorboard / matplotlib）
#   -e .[dev]         开发与 CI（pytest / coverage / black / ruff / mypy / pip-audit）
#   -e .[export]      ONNX 导出与验证
#   -e .[models]      第三方骨干（timm）
#   -e .[datasets]    数据增强（albumentations）
#   -e .[coco]        COCO 评估（pycocotools）
#   -e .[metrics]     可选指标后端（torchmetrics）
#   -e .[full,dev]    全部能力 + 开发工具
-e .[full,dev]
