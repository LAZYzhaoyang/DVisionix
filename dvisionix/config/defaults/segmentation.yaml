# 分割任务默认配置（继承 base.yaml）
_base_: "base.yaml"

task_type: "segmentation"

model:
  name: "simple_segmentation"
  num_classes: 19
  in_channels: 3

loss:
  type: "cross_entropy"
  ignore_index: 255

data:
  root: "./data"
  dataset: "cityscapes"
  image_size: 512